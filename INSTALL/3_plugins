cette Mise En Place nécessite : 
- Git (voir : https://gist.github.com/derhuerst/1b15ff4652a867391f03 )

ce dossier contient les plugins de nagios qui etait encore présent sur la version 3.4 de centreon

pour installer ces plugins (sur une installation ISO centreon) :
(le yum install centreon-* n'installe pas les plugin de nagios vous devez le faire vous même)
(Les chemin sont peut être différent sur vos machine mais le principe restent le même)

```shell
cd /usr/lib/
```

```shell
mkdir nagios
```

```shell
cd nagios
```

```shell
git clone https://github.com/rodeur2225/jubilant-garbanzo.git
```

```shell
mv jubilant-garbanzo/plugins .
```

```shell
rm -rf jubilant-garbanzo
```

le seul moyen de savoir si l'installation fonctionne et de la tester 


__END__
